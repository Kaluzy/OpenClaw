# One-Night Offer Builder (v0)

Static, deterministic prototype to generate:
- niche pick
- offer
- pricing anchor
- 10 outbound messages

No backend, no API keys.

## Dev

Open `index.html`.

## Publish

Use `scripts/publish-prototype.sh` (created in workspace root) to push to `gh-pages` under `/prototype/`.
